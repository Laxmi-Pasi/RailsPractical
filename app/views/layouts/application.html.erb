<!DOCTYPE html>
<html>
  <head>
    <title>RailsPractical</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if logged_in? %>
      <div style="background-color:yellow;text-align:right"> <%= link_to 'Log Out', users_path %> </div>
    <% end %>
    <%= yield %>
  </body>
</html>
